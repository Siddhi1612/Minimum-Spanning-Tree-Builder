# Minimum-Spanning-Tree-Builder
Uses python functions to find the minimum spanning tree either through Kruskal's or Prim's algorithm. It then uses HTML + JavaScript to create a simple graphical user interface.
